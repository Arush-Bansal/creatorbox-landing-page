# Copy to .env.local for local dev (optional)

# GitHub repo that hosts desktop releases (owner/name)
GITHUB_REPO=Arush-Bansal/creatorbox

# Required for private repos (read-only Contents + Metadata is enough).
# Create: GitHub → Settings → Developer settings → Fine-grained PAT
GITHUB_TOKEN=
